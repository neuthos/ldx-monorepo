# Ringi 100 Phase 3 — Slip Cancellation (Arrival, Shipment, Store Sales, Store Sales Return) Test Plan

Source spec: `docs/ringi/specs/2026-08-18-ringi-100-phase-3-cancellation-design.md` (User-Approved design, commit `0831401`).

## 1. Summary

### TL;DR

Test plan for extending the governed two-step slip cancellation (Confirmed → 取消待ち → 取消確定) to four new slip types by replicating the approved phase 1/2 architecture. Highest risks: (1) cross-domain cascade correctness with full-chain reuse (Store Sales → Sales → Purchase; Arrival → Shipment movement pair), (2) inventory/lot/outstanding reversal side effects, (3) batch atomicity vs partial success, (4) regression of the already-approved phase 1/2 cancellation suites. All product decisions resolved (the Store Sales Information list label was decided as "Cancelled" per PRD-StoreSales 2.6). The plan is ready.

### Coverage and confidence

- BR/FR coverage: 20/20 requirement groups (100%), traced via the spec's `PRD-<doc> <section>#<no>` scheme (the PRDs carry section numbers, not formal FR/BR IDs — scheme defined in the spec).
- Test-plan confidence: 86% (estimated, not execution evidence).
- Confidence rubric: requirement traceability 40% → 36 (1:1 table traceability, all 20 groups covered); BOUNDARIES evaluation 20% → 17 (all ten addressed; E partially covered, justified below); blast-radius/domain-effect coverage 20% → 18 (cross-domain chains, EC, customer order, order-master outstanding, phase 1/2 regression all traced); test-level fit 10% → 7 (pyramid deviation, justified below); repository evidence 10% → 8 (codebase-memory graph + disclosed text-search fallback for `ldx_ec`).
- Total test cases: 101 finalized + 0 PENDING.
- Why this is sufficient: every PRD modification item and every PRD scenario-test section (3.1–3.8 × 4 documents) maps to at least one row; every locked decision (full-chain reuse, 取消区分 as separate field, doc-vs-code deltas, Store Sales Information "Cancelled" label) has explicit rows.

### Healthy pyramid

- Target: Unit ~70%, Integration 15–20%, E2E 5–10%.
- Actual (by rows, not steps): Unit 43/101 (43%), Integration 28/101 (28%), E2E 30/101 (30%).
- Assessment: risk-adjusted deviation. The four PRDs mandate ~42 user-level scenario tests (sections 3.1–3.8 per document) that are inherently journey-shaped; each E2E row consolidates 3–10 PRD sub-scenarios (e.g. CXL-072 covers PRD-Arrival 3.1.1–3.1.5), so scenario-weighted coverage skews far more unit-heavy than raw rows suggest. Underlying logic (guards, markers, validation, eligibility, config) is covered at Unit; contracts and cascades at Integration.

### BOUNDARIES summary

- B — Covered: decimal/precision and closing-date boundary (CXL-009, CXL-020, CXL-025, CXL-027).
- O — Covered: no-overwrite ordering, duplicate slip numbers in file, partial-success skip rules, STS/RST mixed split (CXL-008, CXL-025, CXL-028, CXL-034, CXL-062–064).
- U — Covered: JP term preservation, 新規/更新 allowed values, multibyte remarks, EN/JP completeness (CXL-036, CXL-037, CXL-038, download rows).
- N — Covered: tri-state `cancellation_type`, blank slip no, blank details vs `other`, unconsumed-lot-only deletion (CXL-001–004, CXL-020, CXL-025, CXL-039).
- D — Covered: zero-selection button disabled, large upload file, batch chunking (list journeys step 1; CXL-062–064, CXL-066).
- A — Covered: endpoint `require_perm`, batch permission map, master items, four authority levels × four documents (CXL-030, CXL-044–047, CXL-065, CXL-096–097).
- R — Covered: mark-before-cascade cycle safety, double-confirm serialization, concurrent batch+single, atomic upload rollback (CXL-024, CXL-061, CXL-066, CXL-068).
- I — Covered: guard errors as envelope rows (partial success preserved), `ldx_ec`-absent no-op, malformed envelope rejection (CXL-035, CXL-062–064, CXL-069).
- E — Partially covered: locale JP/EN and timezone-aware `cancellation_date` rendering are exercised (CXL-037, download/list rows); no dedicated browser/OS matrix — justified: PRD poses no browser-specific risk beyond the E2E repo's default Playwright browsers.
- S — Covered: valid/invalid transitions, terminal non-re-entry, release-only-from-pending, closing freeze, re-entry via re-mark (CXL-005–007, CXL-009, CXL-026, CXL-061).

### Blast-radius and domain-driven effect summary

Layers: BE services/controllers/schemas, `stock.picking` fields, multi-process wizard, batch engine configs, menu/authority data, download generators, tests; FE services/types, MultiProcessTools type generation, 3 list screens, 3 registration screens, 4 batch-registration pages, download/i18n; E2E page objects + specs. Downstream paths traced into cases: Sales chain (billing/deposit blockers surface from Store Sales/Shipment cancels — CXL-053, CXL-057, CXL-060), Purchase chain (paid blocker from Arrival — CXL-010, CXL-050), EC order/return status flips (CXL-057, CXL-059, CXL-069), Customer Order Create-Shipment re-enable (CXL-023, CXL-055, CXL-084), order-master outstanding recalculation (CXL-021, CXL-051), movement-pair inventory return (CXL-024, CXL-049), inventory/lot/history reversals (CXL-020, CXL-048, CXL-052, CXL-056, CXL-058), phase 1/2 + consignment regression (CXL-070 and suite assumption).

### Assumptions, unknowns, and PENDING decisions

- Requirement IDs follow the spec's `PRD-<doc> <section>#<no>` scheme; PRD-All denotes requirements repeated verbatim across all four documents.
- PRD-Arrival 3.1.2 quotes a shorter error text than 2.1#2 ("not Confirmed" vs "not Confirmed or Pending Cancellation"); the 2.1#2 wording is treated as the requirement for parity with the approved phase 1/2 message.
- `ldx_ec` linkage (`ec.order.receive`/`ec.order.return` → `store_sales_id`) rests on fallback text-search evidence (graph scope is `ldx_addons`); flagged in the spec.
- Spec pin-points (closing-lock date field per model, Create-Arrival/Create-Shipment reactivation flags, lot-deletion query, outstanding recalc entry point, process-code numbering) are implementation-resolved; tests assert observable behavior and will bind fixtures to the resolved fields without changing intent.
- BE repo has no root AGENTS/CLAUDE; BE test conventions follow the existing `test_ringi_100_*` suites (graph evidence).
- E2E repo conventions per its `CLAUDE.md`: Playwright, sequential `--workers 1`, `tests/scenario/` layout, page objects under `pages/inventory-control/`.
- [USER-APPROVED] Store Sales Information list label: "Cancelled" per PRD-StoreSales 2.6 (the underlying value stays 取消確定 / `cancellation_confirmed`; display-label-only difference from other documents, which render "Cancellation Confirmed").
- No repository was modified and no test was executed for this plan.

## 2. Data Preparation Summary

- **Core slips per domain** (graph-resolved models): `stock.picking` `transfer_type='arrival'` and `'shipment'` in `state` `draft` (Waiting) and `done` (Confirmed); `store.sales` `sales_operation_type` `'sales'` (STS-) and `'return'` (RSTS-) in `draft`/`done`. Variants: clean Confirmed, already 取消待ち, already 取消確定, closing-locked (date before a closed period via `change.lock.date.history`).
- **Arrival link fixtures**: linked Purchase `account.move` in Draft / Confirmed+Not Paid / Confirmed+Paid (payment status set via reconciled payment); Order `order.master` Confirmed with arrival created; inter-store movement pair (`inter.store.movement` → shipment done + arrival done, and shipment done + arrival draft).
- **Shipment link fixtures**: Sales `sale.order` Draft; Confirmed with billing Draft; Confirmed with billing Confirmed (blocker); linked arrival Waiting (draft) and Confirmed (blocker); Customer Order `receipt.order` Confirmed.
- **Store Sales link fixtures**: store-sales-return (RSTS) Draft and Confirmed linked via `return_store_slip_id`; `ec.order.receive` Confirmed pointing `store_sales_id`; Sales `sale.order` Confirmed without and with a Sales Return (`warehouse.return.instruction` Confirmed).
- **Store Sales Return link fixtures**: `ec.order.return` Confirmed pointing at the RSTS slip; `warehouse.return.instruction` Confirmed linked via `store_sales_return_id`; original STS slip Confirmed.
- **Inventory/lot/outstanding baselines**: product with free/reserved/total quantities captured before cancel; a lot created by the target arrival and consumed nowhere; a lot shared with another consumer (negative case); `order.master` outstanding quantity captured before arrival cancel.
- **Actors**: users with authority levels Read / Write / Approve / Write+Approve for each of the four new items; a guest user with none.
- **Upload files per domain**: valid rows (New + Update on pending), and single-error variants — blank slip no, unknown no, already-cancelled no, duplicate no, invalid reason, reason `Others` with blank details, mixed valid+invalid (rollback), large file (volume), JP-locale headers 新規/更新 and EN New/Update.
- **Selection sets for batch**: empty, one, many; mixed Confirmed+Pending; mixed STS+RSTS; includes a 取消確定 slip (error).
- **Locale/timezone**: JP and EN sessions; a slip cancelled near midnight UTC to render `cancellation_date` in JST.
- **Reset/cleanup**: synthetic data only; cancellation markers and created slips removed per suite teardown; no shared-environment mutations.

## 3. Test Cases

Feature code: **CXL**. `ARR`=Arrival, `SHP`=Shipment, `STS`=Store Sales, `RST`=Store Sales Return; "×4 services" = `{ArrivalCancellationService, ShipmentCancellationService, StoreSalesCancellationService, StoreSalesReturnCancellationService}`; "×4 domains" = the four slip types.

### Unit — backend services

| Test ID | Functional Requirement Covered | Test Types | Test Categories | Test Scenario | Test Step |
|---|---|---|---|---|---|
| CXL-001 | PRD-Arrival 2.0; PRD-All 2.x-list#2–3 | Unit | StateTransition; Boundary (N) | Arrival eligibility accepts only a Confirmed arrival picking and rejects every other state or transfer type. | 1. Create `stock.picking` rows: arrival+done, arrival+draft, arrival+done+cancellation_confirmed, shipment+done. 2. Call `_is_confirmed` on each. 3. Assert True only for arrival+done. 4. Assert `_check` raises `CancellationGuardError` with column `['state']` for the rejected rows. |
| CXL-002 | PRD-Shipment 2.0; PRD-All 2.x-list#2–3 | Unit | StateTransition; Boundary (N) | Shipment eligibility accepts only a Confirmed shipment picking and rejects other states or transfer types. | 1. Create `stock.picking` rows: shipment+done, shipment+draft, shipment+done+cancellation_confirmed, arrival+done. 2. Call `_is_confirmed` on each. 3. Assert True only for shipment+done. 4. Assert the guard error names `state` for rejects. |
| CXL-003 | PRD-StoreSales 2.0; PRD-All 2.x-list#2–3 | Unit | StateTransition; Boundary (N) | Store Sales eligibility requires `state='done'`, `sales_operation_type='sales'`, and no terminal cancellation. | 1. Create `store.sales` rows: sales+done, sales+draft, sales+done+cancellation_confirmed, return+done. 2. Call `_is_confirmed` per row. 3. Assert True only for sales+done. |
| CXL-004 | PRD-StoreSalesReturn 2.0; PRD-All 2.x-list#2–3 | Unit | StateTransition; Boundary (N) | Store Sales Return eligibility requires `state='done'`, `sales_operation_type='return'`, and no terminal cancellation. | 1. Create `store.sales` rows: return+done, return+draft, return+done+cancellation_confirmed, sales+done. 2. Call `_is_confirmed` per row. 3. Assert True only for return+done. |
| CXL-005 | PRD-All 2.x-list#2–3 | Unit | StateTransition | `draft_cancel` writes the pending markers and audit fields while the slip's main status stays Confirmed. | 1. For each of the ×4 services, take a Confirmed slip. 2. Call `draft_cancel(reason='input_error')`. 3. Assert `cancellation_type='pending_cancellation'`, `cancellation_pic_id` = caller, `cancellation_date` set. 4. Assert main `state` is still `done`. |
| CXL-006 | PRD-All 2.x-list#2–3 | Unit | StateTransition; Negative | `confirm_cancel` accepts only a Pending slip and raises on any other status. | 1. For ×4 services, call `confirm_cancel` on a Confirmed (non-pending) slip. 2. Assert `CancellationGuardError` "Only Pending Cancellation slips can be confirmed." 3. Mark it pending, call again. 4. Assert `cancellation_type='cancellation_confirmed'` and `cancellation_confirmed_pic_id` set. |
| CXL-007 | PRD-All 2.x-list#2–3 | Unit | StateTransition; Negative | `release_cancel` reverts a Pending slip to no cancellation and refuses a terminal slip. | 1. For ×4 services, `draft_cancel` then `release_cancel`. 2. Assert all `cancellation_*` markers cleared. 3. `confirm_cancel` another slip then `release_cancel`. 4. Assert guard error and markers unchanged. |
| CXL-008 | PRD-All 2.x-list#9 | Unit | Boundary (O); Negative | An existing cancellation reason is never overwritten by a later registration. | 1. For ×4 services, `draft_cancel(reason='client_request', details='kept')`. 2. Call `draft_cancel` again (or the update path) with `reason='other'`. 3. Assert `cancellation_reason='client_request'` and details `'kept'`. |
| CXL-009 | PRD-All 2.x-list#10 | Unit | StateTransition; Boundary (B) | A slip dated inside a closed period fails the closing guard on every mutation path. | 1. Close a period covering a Confirmed slip's date (per model's closing date field). 2. For ×4 services, call `_check`. 3. Assert guard error naming the period-closed message and the date column. 4. Call on an open-period slip and assert no raise. |
| CXL-010 | PRD-Arrival 2.0 | Unit | Negative; Contract | A Paid linked Purchase blocks the arrival cancellation with the exact PRD message. | 1. Create arrival+done linked to a Confirmed+Paid `account.move`. 2. Call `_check`. 3. Assert error message "Cancellation is not allowed because the Payment Status of the Purchase Slip is Paid." and column echoed. |
| CXL-011 | PRD-Shipment 2.0 | Unit | Negative | A confirmed billing on the linked Sales Slip blocks the shipment cancellation. | 1. Create shipment+done linked to a Confirmed Sales with billing Confirmed. 2. Call `_check`. 3. Assert guard error "…Billing Status of the Sales Slip is Confirmed." |
| CXL-012 | PRD-Shipment 2.0 | Unit | Negative | A Confirmed linked Arrival blocks the shipment cancellation. | 1. Create shipment+done with a paired arrival in `done`. 2. Call `_check`. 3. Assert guard error "…Arrival Slip is Confirmed." |
| CXL-013 | PRD-StoreSales 2.0 | Unit | Negative | A Confirmed Store Sales Return blocks the Store Sales cancellation. | 1. Create STS+done with a linked RSTS in `done`. 2. Call `_check`. 3. Assert guard error for the confirmed return. |
| CXL-014 | PRD-StoreSales 2.0 | Unit | Negative | A linked Sales Slip that already has a Sales Return blocks the Store Sales cancellation. | 1. Create STS+done linked to Sales+done that has a confirmed `warehouse.return.instruction`. 2. Call `_check`. 3. Assert guard error. |
| CXL-015 | PRD-StoreSales 2.4#8 | Unit | Negative; Contract | The Store Sales guard rejects when the linked return registration was processed, with the PRD-verbatim message. | 1. Create STS+done with linked RSTS+done. 2. Run the batch-upload validation path and the `_check` path. 3. Assert message "Cancellation is not allowed because Store Sales Return Registration has already been processed." on both paths. |
| CXL-016 | PRD-Arrival 2.0 | Unit | Contract | Arrival preview enumerates Order, Shipment, and Purchase rows with correct `block` flags. | 1. Build an arrival with order, confirmed shipment pair, and purchase (unpaid). 2. Call `preview`. 3. Assert rows for `order_slip` (block=false), `shipment` (false), `purchase_slip` (false). 4. Re-run with a Paid purchase. 5. Assert `purchase_slip` row flips to `block=true`. |
| CXL-017 | PRD-Shipment 2.0 | Unit | Contract | Shipment preview enumerates Sales, Arrival, and Customer Order rows with correct flags. | 1. Build a shipment with linked Sales (billing draft), arrival waiting, and customer order. 2. Call `preview`. 3. Assert `sales_slip`, `arrival`, `customer_order` rows present with expected `block=false`. 4. Swap arrival to confirmed and billing to confirmed. 5. Assert both rows flip to `block=true`. |
| CXL-018 | PRD-StoreSales 2.0 | Unit | Contract | Store Sales preview enumerates Store Sales Return, EC order, and Sales rows with correct flags. | 1. Build STS with RSTS draft, EC order confirmed, Sales confirmed (no return). 2. Call `preview`. 3. Assert `store_sales_return` (false), `ec_order` (false), `sales_slip` (false) rows. 4. Flip RSTS to confirmed and add a Sales Return. 5. Assert those rows flip to `block=true`. |
| CXL-019 | PRD-StoreSalesReturn 2.0 | Unit | Contract | Store Sales Return preview enumerates the unchanged Store Sales, EC return, and Sales Return rows. | 1. Build RSTS+done linked to STS+done, `ec.order.return` confirmed, and `warehouse.return.instruction` confirmed. 2. Call `preview`. 3. Assert `store_sales` row present with `block=false` (unchanged), `ec_order_return` and `sales_return` rows present. |
| CXL-020 | PRD-Arrival 2.0 | Unit | Boundary (B,N) | Arrival cancellation deletes only lots created by that arrival that have no other consumer. | 1. Create arrival+done generating lot L1 (unconsumed) and share lot L2 with another done move. 2. Run the confirm path. 3. Assert L1 unlinked/deleted. 4. Assert L2 still exists with its other consumer intact. |
| CXL-021 | PRD-Arrival 2.0 | Unit | Regression | Cancelling an arrival increases the Order Slip's outstanding order quantity back. | 1. Create order with outstanding O before arrival. 2. Confirm arrival (outstanding decreases). 3. Run arrival confirm-cancel. 4. Assert outstanding returns to O. |
| CXL-022 | PRD-Arrival 2.0 | Unit | Regression | Cancelling an arrival reactivates Create Arrival on the Order side. | 1. Create order with arrival created (creation flag consumed). 2. Run arrival confirm-cancel. 3. Assert the order-side create-arrival flag/state allows creating an arrival again. |
| CXL-023 | PRD-Shipment 2.0 | Unit | Regression | Cancelling a shipment re-enables Create Shipment on the Customer Order. | 1. Create `receipt.order` confirmed with shipment created. 2. Run shipment confirm-cancel. 3. Assert the create-shipment control is enabled again (flag cleared). |
| CXL-024 | PRD-Arrival 2.0; Spec full-chain-reuse | Unit | Boundary (R,S) | The movement pair cancels as one chain without loops: arrival marked first, shipment skipped if already cancelled. | 1. Mock `ShipmentCancellationService`. 2. Confirm-cancel an arrival paired with a confirmed shipment. 3. Assert arrival is marked `cancellation_confirmed` before the shipment service is invoked. 4. Pre-cancel the shipment and re-run. 5. Assert the shipment service is not re-invoked and no error. |

### Unit — batch upload and multi-process configuration

| Test ID | Functional Requirement Covered | Test Types | Test Categories | Test Scenario | Test Step |
|---|---|---|---|---|---|
| CXL-025 | PRD-All 2.x-batchreg | Unit | Boundary (N,O,B); Negative | Upload validation rejects each malformed row variant for all four new configs. | 1. For each domain config, run `validate_cancellation_upload` with: blank slip id; unknown id; duplicate id; operation not New/Update; reason outside the enum; reason `other` with blank details. 2. Assert one structured error row per case with correct `row`, `column`, message. 3. Assert no data rows returned on error. |
| CXL-026 | PRD-All 2.x-batchreg | Unit | StateTransition; Negative | Update operation is allowed only for slips already Pending Cancellation. | 1. Validate an Update row against a clean Confirmed slip. 2. Assert error "Update is allowed only for Pending Cancellation slips." 3. Repeat against a pending slip. 4. Assert valid. |
| CXL-027 | PRD-All 2.x-batchreg | Unit | Boundary (B,N,U) | Reason rules: enum-only values, details required only for Others, multibyte remarks accepted. | 1. Validate reason `"misc"` → enum error. 2. Reason `input_error` with blank details → valid. 3. Reason `other` with blank details → error citing details. 4. Reason `other` with multibyte details (e.g. "顧客依頼により取消") → valid. |
| CXL-028 | PRD-All 2.x-batchreg | Unit | Boundary (O); Negative | The same slip cannot appear twice in one file. | 1. Validate a file with the same slip id on rows 2 and 5. 2. Assert an error on row 5 quoting the duplicate rule. |
| CXL-029 | PRD-Arrival 2.1.1; PRD-Shipment 2.1.1; PRD-StoreSalesReturn 2.2 | Unit | StateTransition; Boundary (O) | Multi-process domains select exactly the eligible slips per method and discriminator. | 1. Inspect the eight new method entries' domains. 2. Pending methods: assert `state='done'`, `cancellation_type in (false, pending)` (per phase 1/2 parity), plus `transfer_type`/`sales_operation_type` discriminator. 3. Confirm methods: assert `cancellation_type='pending'` plus discriminator. 4. Assert no method matches the opposite transfer/op type. |
| CXL-030 | PRD-All 2.x-authority | Unit | Access | Each new multi-process method maps to its menu key with the write or approve level. | 1. Read `CANCELLATION_BATCH_PERMISSIONS` for the 8 new methods. 2. Assert each pending method maps to its document's menu with `'write'`. 3. Assert each confirm method maps with `'approve'`. 4. Assert four distinct menu keys used. |

### Unit — frontend

| Test ID | Functional Requirement Covered | Test Types | Test Categories | Test Scenario | Test Step |
|---|---|---|---|---|---|
| CXL-031 | PRD-Arrival 2.1.1; PRD-Shipment 2.1.1 | Unit | Access; Regression | Arrival and Shipment MultiProcessTools entries expose pending/confirm cancel methods gated by ACL. | 1. Render `generateMultiProcessTypes` with `hasWriteAccess/hasApproveAccess` matrices. 2. Assert the arrival and shipment entries list the two new options each. 3. Assert pending disabled without write, confirm disabled without approve. 4. Assert existing options (arrival confirm, purchase create, shipment confirm, sales create) unchanged. |
| CXL-032 | PRD-StoreSalesReturn 2.2 | Unit | Boundary (O); Contract | The store-sales type exposes all four cancel methods discriminated by operation type. | 1. Render the store-sales type entry. 2. Assert four options: STS pending, STS confirm, RST pending, RST confirm. 3. Assert each method's domain filters `sales_operation_type` to its side. 4. Assert labels match i18n keys. |
| CXL-033 | PRD-All 2.x-list#2–3 | Unit | StateTransition; Negative | FE eligibility flags invalid rows per new mode before submit. | 1. Feed row variants (done, draft, pending, cancellation_confirmed) to `getCancellationEligibilityFailures` for modes arrival/shipment/store_sales/store_sales_return. 2. Pending method: assert draft and confirmed-cancel rows land in `invalid_status`/`cancellation_confirmed`. 3. Confirm method: assert only pending rows pass. |
| CXL-034 | PRD-StoreSalesReturn 2.2 | Unit | Boundary (O) | Mixed STS/RST selection splits correctly for each of the four methods. | 1. Feed one STS and one RST row for method `store_sales_pending_cancel`. 2. Assert the RST row flagged invalid. 3. Repeat for `store_sales_return_pending_cancel`. 4. Assert the STS row flagged invalid. |
| CXL-035 | PRD-All 2.x-list#2–3 | Unit | Boundary (I); Contract | The four new FE services post to the right prefixes and reject malformed envelopes. | 1. Instantiate the four services. 2. Assert request URLs use `/v2/arrival/cancel`, `/v2/shipment/cancel`, `/v2/store-sales/cancel`, `/v2/store-sales-return/cancel`. 3. Feed a draft response missing `record_id` and an `affected` row missing `block`. 4. Assert `validateProtectedEnvelope` throws for both. 5. Assert draft with reason `other` and blank remarks throws client-side. |
| CXL-036 | PRD-All 2.x-list#5–6 | Unit | Boundary (U); Contract | New affected entity types render labels and detail links correctly. | 1. Render the preview list with rows `order_slip`, `store_sales_return`, `ec_order_return` plus a legacy `purchase_slip`. 2. Assert each shows its localized label. 3. Assert each number renders as a hyperlink to the correct detail route in a new tab. |
| CXL-037 | PRD-All 2.x-batchreg | Unit | Boundary (U) | Batch sequences define per-domain columns with JP/EN allowed values. | 1. Call `getCancellationBatchSequences` for each domain label. 2. Assert columns: registration operation, slip no, cancellation reason, cancellation details. 3. In EN assert allowed values `['new','update']`; in JP assert `['新規','更新']`. 4. Assert details required only when reason is Others. |
| CXL-038 | PRD-All 2.x-list#4,#8; PRD-All 2.x-list#7 | Unit | Boundary (U) | List presentation renders 取消区分 and reason from raw values via i18n. | 1. Call `getCancellationDisplayFields` with `cancellation_type` false/pending/confirmed and each reason. 2. Assert empty string for false. 3. Assert localized labels otherwise for all supported locales. |
| CXL-039 | PRD-All 2.x-list#5–6 | Unit | Boundary (N); Regression | The reason form stays disabled until a reason is chosen and requires details only for Others. | 1. Render `CancellationReasonForm`. 2. Assert details input and execute disabled initially. 3. Select `input_error`. 4. Assert execute enabled, details optional. 5. Switch to `other` with blank details. 6. Assert execute disabled until details entered. |
| CXL-040 | PRD-All (registration #1); PRD-All 2.x-list#2–3 | Unit | StateTransition | The registration panel shows the right buttons per slip status for the new adapters. | 1. Mount `CancellationPanelCore` with each new service adapter. 2. Confirmed clean → Register visible/enabled, Confirm hidden. 3. Pending → Register (edit-mode) and Confirm visible. 4. Cancellation confirmed → both hidden/disabled. 5. Draft (`isConfirmedStatus=false`) → all disabled. |
| CXL-041 | PRD-All 2.x-list#10 | Unit | StateTransition; Negative | `isBeforeClosingDate` disables both cancellation CTAs. | 1. Mount the panel with `isBeforeClosingDate=true` on a Confirmed slip. 2. Assert Register disabled. 3. Set pending + still locked. 4. Assert Confirm disabled. |
| CXL-042 | PRD-All 2.x-list#7 | Unit | Regression | The five cancellation columns are configured per list with PRD placement. | 1. Read the column config for Arrival, Shipment, and Store Sales lists. 2. Assert the five columns exist. 3. Assert Arrival/Store Sales placement matches PRD (before Past Data; after Registered By / Sales Amount (Excluding Tax) respectively). |
| CXL-043 | PRD-All 2.x-download | Unit | Regression | Download column mapping includes the cancellation classification and five columns for all list and information downloads. | 1. Read the download column mappings for the four documents' list and information downloads. 2. Assert classification + five columns present in each. 3. Assert header keys match i18n labels. |

### Integration — backend

| Test ID | Functional Requirement Covered | Test Types | Test Categories | Test Scenario | Test Step |
|---|---|---|---|---|---|
| CXL-044 | PRD-All 2.x-list#2–3; PRD-All 2.x-authority | Integration | Access; Contract | Arrival endpoints implement the four operations with permission enforcement. | 1. POST `/api/v2/arrival/cancel/preview` for a confirmed arrival → 200 with `affected` array. 2. POST `draft` without cancellation authority → rejected. 3. POST `draft` with write → `success:true`, type pending. 4. POST `confirm` with write only → rejected; with approve → success. 5. POST `release` → markers cleared. 6. Guard case returns `{success:false, message, column, affected}`. |
| CXL-045 | PRD-All 2.x-list#2–3; PRD-All 2.x-authority | Integration | Access; Contract | Shipment endpoints implement the four operations with permission enforcement. | 1–6. Repeat CXL-044 steps against `/api/v2/shipment/cancel/*` with a confirmed shipment. |
| CXL-046 | PRD-All 2.x-list#2–3; PRD-All 2.x-authority | Integration | Access; Contract | Store Sales endpoints implement the four operations with permission enforcement. | 1–6. Repeat CXL-044 steps against `/api/v2/store-sales/cancel/*` with a confirmed STS slip. |
| CXL-047 | PRD-All 2.x-list#2–3; PRD-All 2.x-authority | Integration | Access; Contract | Store Sales Return endpoints implement the four operations with permission enforcement. | 1–6. Repeat CXL-044 steps against `/api/v2/store-sales-return/cancel/*` with a confirmed RSTS slip. |
| CXL-048 | PRD-Arrival 2.0 | Integration | Regression | Confirming an arrival cancellation deducts inventory, recalculates balances, and records the movement history row. | 1. Capture product free/reserved/total before. 2. Run draft then confirm on the arrival. 3. Assert quantities reverted to pre-arrival values. 4. Assert a movement-history transaction labeled "Arrival Cancellation" exists for the slip. |
| CXL-049 | PRD-Arrival 2.0; Spec full-chain-reuse | Integration | Regression | Cancelling the arrival of a completed movement cancels the paired shipment and returns inventory to the origin store. | 1. Build movement pair shipment+done → arrival+done; capture origin/destination stock. 2. Confirm-cancel the arrival. 3. Assert shipment `cancellation_type='cancellation_confirmed'`. 4. Assert destination stock reduced and origin stock restored. 5. Assert propagated remarks reference the arrival on the shipment. |
| CXL-050 | PRD-Arrival 2.0 | Integration | StateTransition | The linked Purchase is deleted when Draft, cancelled when Confirmed+Unpaid, and blocks when Paid. | 1. Arrival+draft-purchase → confirm-cancel → assert purchase unlinked and deleted. 2. Arrival+confirmed-unpaid → assert purchase cancelled with markers. 3. Arrival+paid → assert guard blocks and nothing changed. |
| CXL-051 | PRD-Arrival 2.0 | Integration | Regression | The Order Slip stays Confirmed and Create Arrival is reactivated after arrival cancellation. | 1. Arrival linked to confirmed order. 2. Confirm-cancel the arrival. 3. Assert order `state` unchanged. 4. Assert create-arrival control active again (flag). 5. Assert outstanding recalculated (CXL-021 behavior at DB level). |
| CXL-052 | PRD-Shipment 2.0 | Integration | Regression | Confirming a shipment cancellation adds inventory back and records "Shipment Cancellation". | 1. Capture stock after shipment confirm. 2. Run draft+confirm on the shipment. 3. Assert available/free/total restored. 4. Assert movement-history row "Shipment Cancellation". |
| CXL-053 | PRD-Shipment 2.0; Spec full-chain-reuse | Integration | StateTransition | The linked Sales Slip is cancelled via the full phase-1 chain, or blocks when its billing is confirmed. | 1. Shipment+sales draft → cancel → sales cancelled/deleted. 2. Shipment+sales confirmed, billing draft → cancel → sales cancelled via chain (AR drafted, inventory reverted). 3. Shipment+sales confirmed, billing confirmed → assert guard blocks the shipment cancel with the billing message. |
| CXL-054 | PRD-Shipment 2.0 | Integration | StateTransition | A Waiting linked arrival is deleted and a Confirmed one blocks the shipment cancellation. | 1. Shipment+arrival draft → cancel → arrival removed. 2. Shipment+arrival done → assert guard "…Arrival Slip is Confirmed." and no changes. |
| CXL-055 | PRD-Shipment 2.0 | Integration | Regression | After shipment cancellation the Customer Order's Create Shipment is enabled again. | 1. Customer order confirmed with shipment created. 2. Confirm-cancel the shipment. 3. Assert order stays confirmed. 4. Assert create-shipment flag re-enabled and a new shipment can be created. |
| CXL-056 | PRD-StoreSales 2.0 | Integration | Regression | Confirming a Store Sales cancellation adds inventory back and records "Sales Cancellation". | 1. Capture stock after STS confirm. 2. Draft+confirm the cancellation. 3. Assert stock restored. 4. Assert movement-history row "Sales Cancellation". |
| CXL-057 | PRD-StoreSales 2.0; Spec full-chain-reuse | Integration | StateTransition | Store Sales cancellation cascades per the linked table and blocks on confirmed return or sales-with-return. | 1. STS+RSTS draft → cancel → RSTS cancelled, STS cancelled. 2. STS+RSTS confirmed → blocked. 3. STS+EC order confirmed → EC order status "Cancel", STS cancelled. 4. STS+Sales (no return) → Sales cancelled through the full chain. 5. STS+Sales with confirmed return → blocked. |
| CXL-058 | PRD-StoreSalesReturn 2.0 | Integration | Regression | Confirming a Store Sales Return cancellation deducts the returned inventory and records "Return Cancellation". | 1. Capture stock after RSTS confirm. 2. Draft+confirm the cancellation. 3. Assert stock reduced by the returned qty. 4. Assert movement-history row "Return Cancellation". |
| CXL-059 | PRD-StoreSalesReturn 2.0 | Integration | StateTransition | RSTS cancellation cancels the linked EC return and Sales Return while leaving the Store Sales slip unchanged. | 1. RSTS linked to STS+done, `ec.order.return` confirmed, `warehouse.return.instruction` confirmed. 2. Confirm-cancel the RSTS. 3. Assert EC return cancelled and sales return cancelled (chain). 4. Assert STS still `done` with no cancellation markers. |
| CXL-060 | Spec full-chain-reuse; PRD-StoreSales 2.0 | Integration | Contract | A Sales-side blocker surfaces through the Store Sales preview and cancel with transitive affected rows. | 1. STS linked to Sales with confirmed billing (and a connected purchase). 2. Call preview. 3. Assert affected includes the blocking billing row and the transitive purchase rows. 4. Attempt confirm → guard error with the same affected list. |
| CXL-061 | Spec full-chain-reuse | Integration | Boundary (R,S) | Preview flattens transitively without loops across the movement pair and the STS↔Sales cycle. | 1. Preview a movement-pair arrival. 2. Assert shipment row appears exactly once. 3. Preview an STS linked to Sales that links back to store sales. 4. Assert the originating STS appears once (self excluded) and termination is finite (visited-set). |
| CXL-062 | PRD-Arrival 2.1.1; PRD-Shipment 2.1.1 | Integration | Boundary (O,D) | Pending batch methods process only eligible slips and skip failures per slip. | 1. Select mixed done+pending+draft slips (ARR and SHP separately). 2. Run `arrival_pending_cancel` / `shipment_pending_cancel`. 3. Assert done slips became pending. 4. Assert draft/pending slips skipped with per-slip error rows. 5. Repeat with a selection larger than one chunk to cross `BATCH_PROCESS`. |
| CXL-063 | PRD-Arrival 2.1.1; PRD-Shipment 2.1.1 | Integration | Boundary (O) | Confirm batch methods process only Pending slips and skip others. | 1. Select mixed pending+done slips. 2. Run the confirm methods. 3. Assert pending slips became 取消確定. 4. Assert done slips skipped with error rows. |
| CXL-064 | PRD-StoreSalesReturn 2.2 | Integration | Boundary (O) | The shared Entry-list batch handles mixed STS/RST selections with per-type partial success. | 1. Select 1 STS+done and 1 RSTS+done. 2. Run `store_sales_pending_cancel`. 3. Assert STS pending, RST skipped with error. 4. Run `store_sales_return_pending_cancel` on a fresh pair. 5. Assert RST pending, STS skipped. |
| CXL-065 | PRD-All 2.x-authority | Integration | Access | Multi-process cancel jobs enforce write vs approve per method. | 1. Run a pending method as a write-only user → succeeds. 2. Run a confirm method as write-only → permission error. 3. Run confirm as approve user → succeeds. 4. Run any method as read-only → permission error. |
| CXL-066 | PRD-All 2.x-batchreg | Integration | Boundary (D,R) | One bad row rolls back the entire cancellation upload. | 1. Upload a file with 3 valid rows and 1 invalid (unknown slip). 2. Assert batch marked failed, zero registered. 3. Assert none of the 4 slips changed status. 4. Upload a large valid file (volume) → all rows pending. |
| CXL-067 | PRD-All 2.x-batchreg | Integration | StateTransition | A fully valid cancellation upload marks every slip Pending with audit fields. | 1. Upload valid New rows for confirmed slips of all four domains. 2. Assert each `cancellation_type='pending_cancellation'`, `cancellation_date`/pic set, reason stored. 3. Upload an Update row editing remarks on one pending slip. 4. Assert remarks updated without status change. |
| CXL-068 | PRD-All 2.x-list#2–3 | Integration | Boundary (R) | Concurrent confirm attempts on one slip serialize to one success. | 1. Draft-cancel a slip. 2. Fire two confirm calls concurrently. 3. Assert exactly one success and one guard error. 4. Assert final state 取消確定 with one history/audit trail. |
| CXL-069 | PRD-StoreSales 2.0; PRD-StoreSalesReturn 2.0 | Integration | Boundary (I) | EC cascades no-op safely when `ldx_ec` records/fields are absent. | 1. Create STS/RSTS without EC links in a context without `ldx_ec` fields. 2. Run preview and confirm-cancel. 3. Assert no exception and cancellation completes without EC rows. |
| CXL-070 | Spec Acceptance 5 | Integration | Regression | Phase 1/2 cancellation suites remain green after phase 3 changes. | 1. Run the existing `test_ringi_100_*` suites for sales, sales return, purchase, purchase return. 2. Assert all pass. 3. Run consignment-related suites touched by phase 2. 4. Assert unchanged results. |
| CXL-071 | PRD-All 2.x-download | Integration | Regression; Boundary (U,E) | List and information downloads output the cancellation columns with correct values for cancelled slips. | 1. Cancel slips of each domain (pending and confirmed variants). 2. Generate list download and information download per document. 3. Assert classification column shows 取消待ち/取消確定 (localized) and blank for clean slips. 4. Assert date/registrant/approver/reason/details columns match DB values with timezone-aware dates. |

### E2E — PRD scenario journeys (Playwright, `--workers 1`)

| Test ID | Functional Requirement Covered | Test Types | Test Categories | Test Scenario | Test Step |
|---|---|---|---|---|---|
| CXL-072 | PRD-Arrival 3.1.1–3.1.5; PRD-All 2.x-list#1–6 | E2E | Smoke; Negative | The Arrival list batch-cancellation journey enforces selection, blockers, and reason gating end to end. | 1. Open Arrival Slip List with no selection → Batch Cancellation disabled. 2. Select a Waiting slip and click → error "Slips whose Status is not Confirmed or Pending Cancellation cannot be selected." 3. Select a 取消確定 slip → its selection error. 4. Select a Confirmed slip linked to a Paid purchase → payment-status error. 5. Select a clean Confirmed slip → modal opens (MultiProcessTools-based) with reason form. 6. Choose Others with blank details → Execute disabled. 7. Enter details → Execute enabled → run → slip shows 取消待ち and the reason applied. |
| CXL-073 | PRD-Arrival 3.1.6; PRD-Arrival 2.1.1 | E2E | Boundary (O) | Mixed-status batch cancellation partially succeeds on the Arrival list. | 1. Select one Confirmed and one Pending arrival. 2. Run Batch Arrival Pending Cancellation. 3. Assert the Confirmed slip became Pending. 4. Assert the already-Pending slip reported as failed in the result list. 5. Assert list refreshed showing both states. |
| CXL-074 | PRD-Arrival 3.1.7–3.1.10; PRD-All 2.x-list#4,#7–10 | E2E | Regression; Boundary (U) | Arrival list filters, cancellation columns, no-overwrite, and closing lock behave per PRD. | 1. Filter Status (取消区分) by Pending Cancellation → only pending slips shown. 2. Repeat for Cancellation Confirmed. 3. Open a cancelled slip's row → the five cancellation items display with values. 4. Re-run batch cancel on a slip with an existing reason → reason unchanged after execution. 5. Attempt to edit a closing-locked slip → edit prevented. |
| CXL-075 | PRD-Arrival 3.2.1–3.2.2; PRD-All (registration) | E2E | StateTransition | The Arrival registration screen controls the cancellation buttons and reason flow, with the affected-slip preview modal. | 1. Open a Confirmed arrival → Cancellation Registration enabled, others disabled. 2. Click it → Yes/No modal; choose Yes. 3. Preview modal lists linked slips with hyperlinks opening details in a new tab. 4. Select Others with blank details → error; then Input Error → registration proceeds. 5. Open a Waiting arrival → all three buttons disabled. 6. Open the now-Pending slip → Cancellation Confirmed enabled. |
| CXL-076 | PRD-Arrival 3.3.1–3.3.4; PRD-All 2.x-batchreg | E2E | Negative; Boundary (D) | The Arrival cancellation batch-registration journey validates, rolls back, and applies. | 1. Open Arrival Slip Batch Registration → Registration Operation includes Cancellation with warning text. 2. Download the cancellation sample file. 3. Upload blank-slip-no file → row error. 4. Upload unknown and already-cancelled numbers → respective errors. 5. Upload Others-with-blank-details → error. 6. Upload mixed valid+invalid → total failure, no status changes. 7. Upload all-valid → all slips 取消待ち. |
| CXL-077 | PRD-Arrival 3.4.1, 3.5.1; PRD-All 2.x-download | E2E | Regression | Arrival downloads and the Arrival Information list carry the cancellation data. | 1. Download list data for cancelled arrivals. 2. Assert classification + five columns present with correct values. 3. Open Arrival Information List → status/cancellation item and filter work. 4. Assert the five items display on detail rows. |
| CXL-078 | PRD-Shipment 3.1.1–3.1.5; PRD-All 2.x-list#1–6 | E2E | Smoke; Negative | The Shipment list batch-cancellation journey enforces selection, linked-slip blockers, and reason gating. | 1–2. Repeat CXL-072 steps 1–2 on Shipment Slip List (Draft status case). 3. Select a shipment with billing-confirmed sales → billing error. 4. Select one with confirmed arrival → arrival error. 5. Select 取消確定 → selection error. 6. Select a clean Confirmed shipment → reason modal gating as CXL-072 steps 6–7. |
| CXL-079 | PRD-Shipment 3.1.6–3.1.7; PRD-Shipment 2.1.1 | E2E | Boundary (O) | Both Shipment batch types partially succeed on mixed selections. | 1. Mixed Confirmed+Pending selection → run Batch Shipment Pending Cancellation → Confirmed becomes Pending, Pending skipped. 2. Fresh mixed selection → run Batch Shipment Cancellation Confirmed → Pending becomes 取消確定, Confirmed skipped. |
| CXL-080 | PRD-Shipment 3.1.8–3.1.11; PRD-All 2.x-list#4,#7–10 | E2E | Regression; Boundary (U) | Shipment list filters, columns, no-overwrite, and closing lock behave per PRD. | 1. Filter by 取消区分 options. 2. Verify the five columns on a cancelled shipment's row/detail. 3. Re-cancel with a new reason → original reason kept. 4. Attempt edit on a closing-locked shipment → prevented. |
| CXL-081 | PRD-Shipment 3.2.1–3.2.3 | E2E | StateTransition; Negative | Shipment registration buttons follow status rules and the linked-slip error shows on Cancellation Registration. | 1. Confirmed shipment → Registration enabled. 2. Draft / 取消確定 shipments → all disabled. 3. Pending shipment → Confirmed enabled. 4. On a billing-confirmed linked sales, click Cancellation Registration → billing error displayed. 5. Complete a happy-path registration with reason. |
| CXL-082 | PRD-Shipment 3.3.1–3.3.4 | E2E | Negative; Boundary (D) | Shipment cancellation batch-registration validates, rolls back, and applies. | 1–7. Repeat CXL-076 steps on Shipment Slip Batch Registration, including the linked Sales/Arrival-confirmed slip-number error case (3.3.2#4). |
| CXL-083 | PRD-Shipment 3.4.1, 3.5.1–3.5.2 | E2E | Regression | Shipment downloads and Shipment Information list carry the cancellation data. | 1. Download Shipment list data → columns verified. 2. Open Shipment Information List → filter + items verified. 3. Download information data → classification, Registered By, and the five columns verified. |
| CXL-084 | PRD-Shipment 3.6.1 | E2E | Regression | Create Shipment becomes clickable again on the Customer Order after shipment cancellation. | 1. Cancel a shipment linked to a confirmed Customer Order. 2. Open that order. 3. Assert status remains Confirmed. 4. Assert Create Shipment enabled and clickable. |
| CXL-085 | PRD-StoreSales 3.1.1–3.1.4; PRD-All 2.x-list#1–6 | E2E | Smoke; Negative | The Store Sales Entry batch-cancellation journey enforces selection and reason gating. | 1–3. Repeat CXL-072 steps 1–3 on Store Sales Entry (Draft status case, 取消確定 case). 4. Select a clean Confirmed STS slip. 5. Reason modal gating per CXL-072 steps 6–7. 6. Select multiple slips → reason applied to all. |
| CXL-086 | PRD-StoreSales 3.1.6–3.1.9; PRD-All 2.x-list#4,#7–10 | E2E | Regression; Boundary (U) | Store Sales Entry filters, columns (inserted per PRD placement), no-overwrite, and closing lock behave per PRD. | 1. Filter by 取消区分 options. 2. Verify the five items between Sales Amount (Excluding Tax) and Past Data. 3. Re-cancel with existing reason → unchanged. 4. Closing-locked slip → edit prevented. |
| CXL-087 | PRD-StoreSales 3.2.1–3.2.2 | E2E | StateTransition | Store Sales registration buttons and reason flow follow the status rules. | 1. Confirmed STS → Cancellation Registration enabled. 2. Draft / 取消確定 → all disabled. 3. Pending → Cancellation Confirmed enabled. 4. Happy-path registration with Others+details and Input Error variants. |
| CXL-088 | PRD-StoreSales 3.3.1–3.3.4; PRD-StoreSales 2.4#8 | E2E | Negative; Boundary (D) | Store Sales cancellation batch-registration validates all error variants including the linked-return guard. | 1–6. Repeat CXL-076 steps on Store Sales Batch Registration. 7. Upload a slip whose linked return is confirmed → "Cancellation is not allowed because Store Sales Return Registration has already been processed." |
| CXL-089 | PRD-StoreSales 3.4.1, 3.5.1 | E2E | Regression | Store Sales downloads and Information list carry the cancellation data. | 1. Download list data → classification + five columns between Registered By and Remarks. 2. Information list → items and filter verified. 3. Values match the DB. |
| CXL-090 | PRD-StoreSalesReturn 3.1.1–3.1.4 | E2E | Smoke; Negative | The Store Sales Return batch-cancellation journey enforces selection and reason gating. | 1–4. Repeat CXL-085 steps on the shared Entry list targeting RSTS slips (Draft status case, 取消確定 case, reason gating). |
| CXL-091 | PRD-StoreSalesReturn 3.1.5; PRD-StoreSalesReturn 2.2 | E2E | Boundary (O) | Mixed STS/RST selection partially succeeds per selected batch type. | 1. Select one STS and one RSTS (both Confirmed). 2. Run Batch Store Sales Return Pending Cancellation. 3. Assert RSTS became Pending and STS reported failed (partial success). 4. Run Batch Store Sales Pending Cancellation on a fresh pair → inverse outcome. |
| CXL-092 | PRD-StoreSalesReturn 3.1.6–3.1.9 | E2E | Regression; Boundary (U) | Return list filters, columns, no-overwrite, and closing lock behave per PRD. | 1. Filter by 取消区分 options. 2. Verify the five items on cancelled RSTS rows. 3. Re-cancel with existing reason → unchanged. 4. Closing-locked RSTS → edit prevented. |
| CXL-093 | PRD-StoreSalesReturn 3.2.1–3.2.2 | E2E | StateTransition | Return registration buttons and reason flow follow the status rules. | 1. Confirmed RSTS → Registration enabled. 2. Draft / 取消確定 → disabled. 3. Pending → Confirmed enabled. 4. Happy path with both reason variants. |
| CXL-094 | PRD-StoreSalesReturn 3.3.1–3.3.4 | E2E | Negative; Boundary (D) | Return cancellation batch-registration validates, rolls back, and applies. | 1–7. Repeat CXL-076 steps on the return side of Store Sales Batch Registration (RSTS numbers). |
| CXL-095 | PRD-StoreSalesReturn 3.4.1, 3.5.1 | E2E | Regression | Return downloads and Information list carry the cancellation data. | 1. Download list data → columns verified. 2. Information list → items and filter verified. 3. Values match the DB. |
| CXL-096 | PRD-All 2.x-authority (master screens) | E2E | Access | The four new authority items appear in User ID Master and Group Authority Master. | 1. Open User ID Master edit. 2. Assert 【Arrival Slip Cancellation】, 【Shipment Slip Cancellation】, 【Store Sales Slip Cancellation】, 【Store Sales Return Slip Cancellation】 items exist under their PRD groups. 3. Repeat in Group Authority Master. 4. Assert guest users hold none of them. |
| CXL-097 | PRD-All 2.x-authority (behavior) | E2E | Access | Each authority level gates the cancellation operations correctly across the four documents. | 1. As Read user → cancellation buttons absent/disabled on all four registration screens. 2. As Write user → registration (Pending) works, Cancellation Confirmed disabled, on all four. 3. As Approve user → can confirm a Pending slip, cannot register. 4. As Write+Approve → full flow on all four. |
| CXL-098 | PRD-Arrival 3.7.1; PRD-Shipment 3.8.1; PRD-StoreSales 3.7.1; PRD-StoreSalesReturn 3.7.1 | E2E | Regression | Backend data integration is observable after cancellation confirms. | 1. Capture inventory values on the inventory screen. 2. Confirm-cancel one slip per domain. 3. Assert quantities revert per domain direction. 4. Open movement history → assert the four cancellation transaction labels visible. |
| CXL-099 | PRD-Arrival 3.8.1; PRD-Shipment 3.8.2 | E2E | Regression | Linked-slip integration is visible: cancelled partners show their new status and preview hyperlinks open details. | 1. Preview an arrival cancellation on the registration screen. 2. Assert linked shipment/purchase rows listed with numbers as hyperlinks opening details in a new tab. 3. Confirm the cancellation. 4. Open the linked slip's list → status shows cancelled per the cascade matrix. |
| CXL-100 | Spec doc-vs-code deltas (locked) | E2E | Negative; Contract | The batch flow shows no affected/blocker modal, while the registration screen does. | 1. Run a batch cancellation from each list → assert no affected-slips or blocker modal appears; failures appear in the multi-process result list. 2. Open a registration screen → Cancellation Registration preview modal lists affected slips with block indicators. |
| CXL-101 | PRD-StoreSales 2.6; PRD-StoreSalesReturn 2.4 | E2E | Boundary (U) | The Store Sales Information list renders the cancelled-status label as "Cancelled" (USER-APPROVED) while other documents use "Cancellation Confirmed". | 1. Create a 取消確定 Store Sales slip. 2. Open Store Sales Information. 3. Assert the status/cancellation item shows "Cancelled" for that slip. 4. Assert the filter option reads "Cancelled". 5. Assert the Store Sales Information download uses the same label. 6. Open Shipment Information for contrast → its equivalent shows "Cancellation Confirmed". |
